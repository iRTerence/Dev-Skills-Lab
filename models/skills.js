let skills = ['Java Script', 'CSS', 'HTML', 'jQuery', 'Bootstrap', 'node.js']


function getAll() {
    return skills;
}

function getId(i) {
    return skills[i]
}

module.exports = {
    getAll: getAll,
    getId: getId
}

const idx = skill.findIndex

function skill(){
    var count = 0
    

}
